Practicing practices from O'Reily Apache Kafka Series - Kafka Connect Hands-on Learning by
Stéphane Maarek.

Use the Docker image for Kafka that he recommends and from his github account will be the docker-compose 
instuctions that he uses to run the services of the connectors. 

# run docker-compose up <name-of-you-kafka-cluster>

# get into interaction mode with the container and mount files into the container
# run the below command to run a container instance and to interact with it

# docker run --rm -it -v "$(pwd)":/tutorial --net=host landoop/fast-data-dev bash

create topics for kafka
Local host can be changed to the public ip address from the cloud provider
# kafka-topics --create --topic demo-1-standalone3 --partitions 3 --replication-factor 1 --zookeeper 127.0.0.1:2181

run the connection configurations 
connect-standalone worker.properties file-stream-stand-alone-demo.properties